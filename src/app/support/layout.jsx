import React from "react";

export const metadata = {
  title: "Support - Techtoniclabs",
  description: "Reach our support team anytime, anywhere.",
};

const Layout = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
